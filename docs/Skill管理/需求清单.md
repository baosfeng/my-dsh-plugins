---
title: 需求清单
description: dsh-my-skill-manager 插件需求清单 — 每次开发/修改本插件前逐条对照，改动不得破坏任何一条既有需求
created: 2026-08-26
updated: 2026-09-02
---

# dsh-my-skill-manager 需求清单

> ⚠️ **每次开发本插件的新功能或修复前，必须逐条对照本清单**：新改动不得破坏任何一条既有需求。

## 1. 核心需求

| #   | 需求                                                                                                                                                                                       | 现状                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 验证方式                                                                                                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | **Skill 列表（分「全局 / 项目」两维度）**：每个 skill 显示名称、描述、来源（全局：`user-dsh`/`user-agents`/`custom`/`bundled`；项目：`project-dsh`/`project-agents`）、状态（启用/已禁用） | client `SkillManagerView` 分段控件切换视图（全局/当前项目，会话 cwd 自动感知，issue #69）+ `GET /my-skill-manager/api/list` 合并官方 catalog 与目录扫描；**项目视图（cwd 非空）只返回项目来源条目，全局 skill 只在全局视角显示**（issue #29）                                                                                                                                                                                                                                                                                                                                  | `test/client-render.mjs`、`test/host-api.mjs`                                                                                                                                     |
| R2  | **按项目启用/禁用**：项目配置 `<项目根>/.dsh/skills.enabled.json`（随仓库版本化）；全局配置 `$DSH_HOME/skills.enabled.json`；全局与项目分开管理                                            | `config.js` 双作用域读写 + `PUT /my-skill-manager/api/config`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `test/host-provider.mjs`、`test/host-api.mjs`                                                                                                                                     |
| R3  | **禁用即不注入**：禁用的 skill 不再注入该项目会话——模型不可见、不可加载                                                                                                                    | rank-0 占位 provider 覆盖合并目录；`get()` 返回 `undefined` 拒绝加载正文                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `test/host-provider.mjs`（rank/description/provider 断言）                                                                                                                        |
| R4  | **项目覆盖全局**：全局 skill 也可在项目内单独禁用                                                                                                                                          | `disabledNamesOf(cwd)` 合并 `global.disabled ∪ project.disabled`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `test/host-provider.mjs`                                                                                                                                                          |
| R5  | **即时生效**：保存配置后 skill 目录缓存失效重算，无需重启                                                                                                                                  | API 保存后调用 `control.invalidate()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `test/host-api.mjs`（invalidate 计数）                                                                                                                                            |
| R6  | **配置容错**：配置文件缺失/损坏降级为空配置，不崩溃                                                                                                                                        | `readConfigFile` try/catch + `normalizeConfig`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `test/host-provider.mjs`                                                                                                                                                          |
| R7  | **API 安全**：`/my-skill-manager/api/*` 只接受回环/可信 host，拒绝跨站                                                                                                                     | `fence.js`（同 /api 网关契约）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `test/host-api.mjs`（fence 用例）                                                                                                                                                 |
| R8  | **误报控制**：未在配置中的 skill 不产生任何候选，未配置时目录与原样一致                                                                                                                    | provider `list()` 仅对被禁用的名字返回占位                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `test/host-provider.mjs`                                                                                                                                                          |
| R9  | **手动刷新**：新建 skill 后点「刷新」立即可见，无需重启                                                                                                                                    | `GET /my-skill-manager/api/rescan`（`control.invalidate()` 失效官方目录缓存后重新扫描）+ client 标题区唯一刷新按钮（加载旋转 + disabled，issue #69）                                                                                                                                                                                                                                                                                                                                                                                                                           | `test/host-api.mjs`（rescan 用例）、`test/client-render.mjs`（刷新按钮）                                                                                                          |
| R10 | **扫描诊断 + 目录补充**：被官方扫描器跳过的条目有提示（缺失名单 + 原因）；目录中 frontmatter 正常的条目显示在列表并标记「未收录」                                                          | `lib/diagnose.js` 独立扫描目录（跟随符号链接）：正常条目进列表（`cataloged: false`），异常条目进 `diagnostics.missing`（broken-symlink / missing-skills-md / missing-frontmatter / missing-name-description / invalid-name），client 渲染未收录 meta 小字与折叠诊断条（issue #69）                                                                                                                                                                                                                                                                                             | `test/diagnose.mjs`、`test/host-api.mjs`（诊断用例）、`test/client-render.mjs`（未收录渲染）                                                                                      |
| R11 | **使用统计**：记录每个 skill 被加载/使用的情况（使用次数累计 + 最近使用时间 + 使用来源）；面板显示次数/最近使用（可排序）+「未使用」过滤；统计持久化重启不丢                               | `lib/usage.js` 统计 store（`$DSH_HOME/skills.usage.json`，防抖 500ms + 原子写 + 启动加载合并）；包装 `ctx.skills.get()` 成功加载即计数，`tools/pre-execute`（skill 工具）标记 model 来源、其余记 user；`GET /list` 响应附带 `usage`；client 列表区排序控件（名称/次数/最近）+「未使用」过滤按钮，行 meta 显示「使用 N 次 · 最近 MM-DD HH:mm · 来源」（issue #91）；（issue #1xx）状态编码只保留开关（移除"启用/已禁用" chip 双编码）、排序/过滤控件提升到 22px/12px 字号与 hover 反馈、meta 行拆分（来源一行 + 使用统计一行）、「未收录」改为行头 warn 徽章（带 tooltip 说明） | `test/usage.mjs`（计数/去重/持久化/重启恢复/容错）、`test/host-api.mjs`（计数来源/禁用不计数/落盘）、`test/client-render.mjs`（渲染/排序/过滤/无状态 chip）、Gherkin 使用统计场景 |
| R12 | **资源护栏（issue #198）**                                                                                                                                                                 | 接入 dsh-shared `createWriteScheduler`（防抖 500ms + 最小间隔 1s + 串行链）替换自写的 `persistSoon` + `dirtyChain` 样板；落盘 `atomicWriteJson` 用**默认护栏**（1s 节流兜底 + 1MB 字节上限——usage 状态自身有界：条目数 = 已知 skill 数）；新增 `drainUsage(store)` 作为确定性就绪信号（测试不再 sleep 猜窗口）                                                                                                                                                                                                                                                                 | 防抖窗口内 100 次 `recordUsage` 合并为 1 次写且计数不丢；`await drainUsage(store)` 后磁盘与内存一致；`flushUsage` 仍立即落盘（teardown/退出前冲刷）                               | `test/usage.mjs`（drainUsage 就绪信号、防抖合并用例） |

## 2. 用户明确强调过的点

- **纯官方依赖**（issue #22 精神）：client 面板挂在官方 `slots` 扩展点（`settings.plugins.tab`），不依赖 dsh-better-sidebar；server 端只用官方 `ctx.skills` / `webServer` / `webRuntime` 服务。
- **宁可漏报也不误报**（继承文件活动插件原则）：禁用名单只含用户显式配置的名字；诊断只报「目录条目存在但 catalog 缺失」且能给出原因的条目，`customSkillDirs`/`bundledSkillDir` 等插件侧不可知的目录不纳入诊断。

## 需求变更记录

- **2026-09-12**：issue #198 落地——新增 R12 资源护栏：usage 落盘改用 dsh-shared `createWriteScheduler` + `atomicWriteJson` 默认护栏，删除插件内 `persistSoon`/`dirtyChain`/`persistTimer` 样板，新增 `drainUsage()` 确定性就绪信号。
- **2026-09-02**：issue #91 落地——新增 R11 使用统计：包装 `ctx.skills.get()` 计数（成功加载即 +1，`tools/pre-execute` 标记模型来源、其余记用户），持久化 `$DSH_HOME/skills.usage.json`（防抖 + 原子写 + 启动加载合并，重启不丢）；`GET /list` 附带 `usage`；client 列表区新增排序控件（名称/次数/最近）与「未使用」过滤，行 meta 显示使用次数/最近时间/来源。
- **2026-09-01**：issue #69 设置页重设计——移除路径输入框/文件夹按钮/说明小字，只保留标题区唯一刷新按钮；全局/项目改为分段控件切换（新增 `GET /api/session` 按会话 cwd 自动感知，项目 tab 仅 cwd 非空时出现，项目根路径只读展示）；列表行重排（名称+状态 chip 文字/颜色双编码为主、描述次之、来源/未收录弱化为 meta 小字）；诊断区改可折叠警示条（仅异常时出现）。R1/R9/R10 功能语义不变。
- **2026-09-02**：issue #29 落地——R1 项目视图过滤（cwd 模式只显示项目来源）、新增 R9 手动刷新（rescan API + 刷新按钮）、R10 扫描诊断 + 目录补充（host 层 `skill-filesystem` 被官方禁用，面板独立扫描目录：正常条目补充进列表并标记「未收录」，异常条目进诊断名单）。
- **2026-08-26**：v0.1.0 建立本清单（issue #23 落地）。
