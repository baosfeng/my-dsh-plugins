---
title: 扩展开发
description: 本仓库新增插件（扩展）的形态约定与七步流程；宿主扩展点语义以官方参考源为准
---

# 扩展开发

> ⚠️ **何时阅读：** 开发扩展（新插件）前必读。
> 宿主扩展点（slots / sidebarRightTabs / documentPreviews / settings / webServer）的**语义与签名查官方参考源**，本仓库不维护副本；本仓库实测的侧边栏调用形状与 DOM 契约见[宿主原生侧边栏扩展点](宿主原生侧边栏扩展点.md)。

## 形态约定

本仓库是 DSH 插件集合仓库，**插件即是扩展**：每个插件是 `plugins/<name>/` 下的自包含 Cordis bundle，通过 `cordis.patch.yml` 挂载到 DSH 宿主。插件经 **Cordis 服务名**消费宿主能力，**无需 require 任何第三方侧边栏包**。

## 七步

1. **搭骨架**：在 `plugins/<新插件名>/` 建自包含插件包（package.json + cordis.patch.yml + lib/ + README + CHANGELOG + LICENSE），可复制现有插件作参照。
2. **写 package.json**：包名 `dsh-<功能>`，`dsh.bundle.patch` 指向 cordis.patch.yml，peer 依赖声明 cordis / react。
3. **写 Server 端** `lib/index.js`：`export const name / inject / apply(ctx)`；`ctx.on()` 监听事件；`ctx.webServer.register()` 注册路由（handler 先做 loopback 信任围栏）。
4. **写 Client 端** `lib/client.js`：`window.__ModuleLoader__.load` 格式；页签走「tab 类型 + keyed 席位」两步式注册，key 取 `definition.id`（见[侧边栏契约](宿主原生侧边栏扩展点.md)）。
5. **写测试**：`test/` 下放纯 Node 冒烟测试（mock ctx / webServer / slots / sidebarRightTabs）。
6. **本地验证**：`dsh plugin --profile web add link:<路径>/plugins/<新插件名>` → 硬刷新浏览器。
7. **发布**：版本号 + CHANGELOG → tag `<包名>@v<版本>` → GitHub Release（见[发版流程](发版流程.md)）。

## 相关文档

→ [插件开发技能](../插件开发技能/概述.md) · [宿主原生侧边栏扩展点](宿主原生侧边栏扩展点.md) · [官方 UI 组件库](官方UI组件库.md) · [发版流程](发版流程.md)
