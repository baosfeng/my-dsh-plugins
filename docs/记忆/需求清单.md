---
title: 需求清单
description: dsh-my-memory 插件需求清单 — 每次开发/修改本插件前逐条对照，改动不得破坏任何一条既有需求
created: 2026-09-03
updated: 2026-09-12
---

# dsh-my-memory 需求清单

> ⚠️ **每次开发本插件的新功能或修复前，必须逐条对照本清单**：新改动不得破坏任何一条既有需求。

## 1. 核心需求

| #   | 需求                                                                                                                                                                                                                            | 现状                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 验证方式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1  | **两级记忆**：全局记忆（跨项目，如用户偏好、常用习惯）+ 项目记忆（按项目 cwd 隔离，如项目约定、技术栈决策），分开存储、分开显示                                                                                                 | 全局 `$DSH_HOME/memory.json` + 项目 `$DSH_HOME/memory/projects/<项目 id>.json`（issue #108 集中存 `$DSH_HOME`，项目 id = 项目根路径 sha256 前 12 位；`findProjectRoot` 向上找 `.git`）；client 面板全局/项目双 section 并排显示                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `test/store.mjs`、`test/host-api.mjs`、`test/client-render.mjs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R2  | **系统提示词注入**：会话开始时把全局记忆注入系统提示词（`ctx.systemPrompt.section`，order 靠前），agent 始终携带用户关键偏好                                                                                                    | `dsh-my-memory` section（order -95，persona 0 之前、dsh-think-zh -90 之前），text 为 provider 函数——每次组装系统提示词时读取全局记忆缓存，注入最新 `maxItems` 条（默认 5），每条**按语义截断** `maxDescLength` 字符（默认 200，优先概要/首句、不截断在句子中间，issue #105）；空记忆渲染空 section（renderPrompt 自动丢弃，零成本）；记忆变更后下一轮组装即时生效，无需重启                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `test/prompt.mjs`、Gherkin 场景 4、场景 10                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R3  | **可视化面板**：设置页签（官方 slots 扩展点）查看全局/项目记忆，支持修改和删除                                                                                                                                                  | `settings.plugins.tab` 页签（id `my-memory`，order 92），全局/项目分区显示（项目 section 弱化 accent 描边 + 项目根徽标），条目支持编辑/删除，新增表单；**打开面板自动加载当前会话项目的记忆**（issue #104，`GET /my-memory/api/session?sessionId=…` 解析会话 cwd，复用 `memory_query` 的会话 cwd 定位逻辑；无项目会话时项目区块给出输入路径的友好空状态提示），仍可手动输入其他项目根路径切换查看；视觉设计（issue #110 + #1xx 修正）：条目卡片化（背景+边框+圆角）、长条目截断+展开、徽标显示数量（不重复 scope 标签；项目含项目根路径）、操作图标组与内容用分隔线分离 hover 高亮、更新时间相对化（「3 分钟前」）+ 按更新时间排序、空/加载/错误状态视觉统一；confidence 缺失/非法时不渲染"置信度 undefined"徽标；项目 section 去除强 accent 底色（只留弱描边）；`--dsw-alias-state-warning-primary` → `-warn-`（原 token 不存在导致取色失效）；路径/新增输入框补 aria-label | `test/client-render.mjs`（含 confidence 缺失回归）、`test/client-session.mjs`、Gherkin 场景 6                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| R4  | **写操作需用户同意**：新增和删除记忆必须得到用户确认，不能静默执行                                                                                                                                                              | 服务端 `POST /my-memory/api/memory` 强制 `confirmed: true` 标记（缺失即 400 拒绝）；client 面板所有写操作先弹自定义确认面板，确认后才携带标记提交                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `test/host-api.mjs`（无标记 400）、Gherkin 场景 1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R5  | **工具查询**：`memory_query` 工具（`ctx.tools.register`）查询记忆详情，只读                                                                                                                                                     | 全局/项目 scope 过滤 + 关键词过滤；项目 cwd 取会话工作目录（`exec.agent.session.header.cwd`）或显式参数；只读不修改任何状态；输出 schema 由共享常量 `MEMORY_ITEM_SCHEMA` 生成（`src/tool.ts` 导出），与 store 真实条目形状同源——含 `category/source/confidence/relatedIds/history/status`（issue #191）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `test/tool.mjs`、`test/tool-output-schema.mjs`（真实返回值过严格 schema 校验，#191）、Gherkin 场景 5                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| R6  | **自定义确认 UI**：面板的新增/删除确认不用原生 ask，基于 ask 改造——删除确认醒目（红色）、保存/新增确认绿色、项目与全局分区显示区别更大                                                                                          | ask 范式确认卡（`.dsh-my-memory-ask`，issue #193）：条带（8px 状态点 + 标题 + 范围徽标）+ 20px 大卡 + 40px/12px 范围选项行 + footer（左反馈 + 右动作）；删除危险色（`--dsw-alias-state-error-primary`）+ 二次确认（首次点击只 arm），保存成功色。**面板内与工具侧共用同一组件**：工具侧经 `conversation.composer` chain（priority 0）接管宿主原生审批卡，仅 `memory_save`/`memory_delete` 当选（其余一律让位宿主）；项目 section 蓝色 accent + 项目根徽标与全局区分                                                                                                                                                                                                                                                                                                                                                                                                          | `test/confirm-ask-ui.mjs`（接管判据 + 不误伤 + 异常让位 + 二次确认 + 视觉契约）、`test/client-render.mjs`（确认面板渲染 + confirmed 标记断言）、真实浏览器（隔离实例：保存/删除各一次 + 其它工具审批不误伤 + danger-full-access 无卡，截图 `assets/issue-193-*.png`）                                                                                                                                                                                                                                                                                                    |
| R7  | **持久化重启不丢**：原子写 + 防抖，重启后恢复                                                                                                                                                                                   | `writeMemoryFile` tmp+rename 原子写；`createStore` 防抖 300ms 合并写盘；启动时 `load()` 恢复缓存（写操作 await 恢复完成，避免竞态覆盖）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `test/store.mjs`（重启恢复、防抖合并、原子写）、`test/host-api.mjs`（防抖落盘）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R8  | **API 安全**：`/my-memory/api/*` 只接受回环/可信 host，拒绝跨站                                                                                                                                                                 | `fence.js`（同 /api 网关契约）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `test/host-api.mjs`（fence 用例）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R9  | **配置容错**：记忆文件缺失/损坏降级为空列表，不崩溃                                                                                                                                                                             | `readMemoryFile` try/catch + `normalizeMemory`（只保留格式正确的条目）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `test/store.mjs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R10 | **工具保存记忆（issue #107）**：agent 可通过 `memory_save` 工具主动保存记忆，经用户确认后才写入（不违反「记忆绝不静默变更」）                                                                                                   | `memory_save` 工具（scope/desc 必填、cwd 可选，项目 cwd 取会话工作目录或显式参数）直接写 store；`tools/pre-execute` 确认门对每次 `memory_save` 调用返回 `{ kind: 'ask', reason }` 触发 DSH 原生审批（approval.request），用户确认后才执行写入，其余工具透传下游决策；保存后内存缓存即时更新——`memory_query` 立即可查、系统提示词注入后续会话生效；`proactivePropose` 配置（默认关，#78 阶段）只影响工具描述是否引导 agent 主动提议保存；输出 schema 声明完整真实条目（`category/source/confidence/relatedIds/history/status`），保存成功不再被宿主 `ToolOutputError` 拒绝（issue #191 修复：此前只声明 4 个基础字段，与 store 返回值不匹配）                                                                                                                                                                                                                                 | `test/tool.mjs`（工具注册/schema/写入生效/确认门/会话 cwd）、`test/tool-output-schema.mjs`（真实 save/query 返回值过严格 schema 校验，#191）、`test/host-api.mjs`（apply 注册 + 确认门集成）、Gherkin 场景 6-8                                                                                                                                                                                                                                                                                                                                                           |
| R11 | **项目记忆集中存储（issue #108）**：项目记忆从 `<项目根>/.dsh/memory.json` 迁移到 `$DSH_HOME` 下集中位置，项目目录不产生 `.dsh/`，旧数据自动迁移不丢失                                                                          | 项目记忆存 `$DSH_HOME/memory/projects/<项目 id>.json`（项目 id = 项目根路径 sha256 前 12 位，按项目隔离语义不变）；首次访问自动检测旧文件并迁移（复制 → 删除旧文件与空 `.dsh` 目录，best-effort），记忆不丢；全新项目无旧文件直接新建；dsh-my-skill-manager 同为项目目录存储模式，是否一并集中化另行评估（README 注明）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `test/store.mjs`（迁移复制/清理/跳过）、`test/host-api.mjs`（首次访问自动迁移集成）、`test/tool.mjs`（真实路径保存）                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| R12 | **记忆内容精简（issue #105）**：保存引导浓缩 + 面板概要/详情两级展示 + 注入按语义截断（可配置 `maxEntryLength`），与 #78 自动提取共用精简约束、预留接口                                                                         | 纯函数 `lib/memory-text.js`：`firstSentence`/`summarizeDesc`/`isOverEntryLimit`（首句摘要，不截断在句子中间；#78 自动提取可直接复用）；保存引导：输入框 placeholder「建议 1-2 句话概括」+ 超 `maxEntryLength`（默认 50）显示「内容过长，建议精简为 1-2 句」+ 确认面板显示概要预览（删除/新增/修改写操作仍必须用户确认）；面板列表**总是显示概要（首句）**、点击展开查看完整详情（存储保留完整 desc，展示层两级）；注入按语义截断优先概要/首句；`GET /my-memory/api/config` 暴露 `maxEntryLength`/`maxDescLength` 供面板使用；`memory_save` 工具描述引导「浓缩为 1-2 句」；超长内容保存**不拒绝**（引导而非硬限制，完整 desc 保留）                                                                                                                                                                                                                                           | `test/memory-text.mjs`（首句/语义截断/超长判断）、`test/prompt.mjs`（注入不截断句子中间）、`test/host-api.mjs`（/config + 超长保存保留完整 desc）、`test/client-render.mjs`（概要渲染/展开/超长提示/概要预览）、Gherkin 场景 10-11                                                                                                                                                                                                                                                                                                                                       |
| R13 | **自动提取记忆候选（issue #78 阶段 1）**：会话结束后从对话内容自动提取记忆候选（偏好/事实/项目/技术栈/工作流），进入「待确认」列表，用户确认后才写入；可关闭自动学习（`autoLearn`，默认关）；提取方式可配置（`extractor: 'rule' | 'llm'`，rule 为本仓库确定性规则提取器，llm 为预留占位）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 监听 `session/event`（user/message，过滤插件注入）只读收集本次会话用户消息 + `agent/status` idle（仅顶层 agent）触发提取；`lib/extract.js` 规则提取器（5 类规则模式 + 项目性关键词 → scope 建议 global/project + 单会话上限 + 去重）；候选独立存 `$DSH_HOME/memory/candidates.json`（`lib/store.js` 的 `createCandidatesStore`，原子写+防抖+重启恢复），与正式记忆完全隔离——正式记忆文件只含用户确认过的条目。**记忆绝不静默变更**（延续 R4）：提取只产生候选，确认（`POST /my-memory/api/candidates/confirm`）/拒弃（`dismiss`）都要 `confirmed: true` 标记，缺失即 400 | `test/extract.mjs`（规则命中/未命中/llm 占位/scope 建议/去重/上限）、`test/candidates.mjs`（收集→提取→落盘、autoLearn 开/关、子代理不触发、插件消息过滤、非 idle 不触发）、Gherkin 场景 12 |
| R14 | **结构化索引（issue #78 阶段 2）**：记忆条目带分类/来源/置信度/更新时间/演进历史元数据，面板可查看来源与演进；存储兼容旧数据                                                                                                    | 条目元数据 `category`（偏好/事实/项目/技术栈/工作流）/`source`（会话 id + 时间）/`confidence`（多次出现提升，上限 5）/`updatedAt`/`relatedIds`/`history`（演进记录）/`status`（矛盾标记）；`lib/store.js` 的 `normalizeMemory`/`withDefaults`（`lib/memory-scoring.js`）对旧数据（无元数据）回退默认值——**旧条目不丢不崩**；`POST /my-memory/api/memory` 的 add **只写 desc**（面板新增的条目分类恒为默认 `fact`、来源为空——**面板不是「按类型保存」入口**；按类型保存走 `memory_save` 的 `category` 参数，见 R18；本条原文曾称 add 支持可选 meta（category/source），与 `src/api-route.ts` 的 `applyAdd` 实现不符，issue #192 修正）；面板条目卡片显示分类徽标 + 置信度 + 矛盾警示 + 「演进历史」展开（`src/client/parts/view-rows.ts` 的 `MetadataRow`）                                                                                                                   | `test/store.mjs`（元数据默认值/兼容/保留）、`test/memory-scoring.mjs`（withDefaults/主题 key）、`test/host-api.mjs`（专题条带元数据）、`test/candidate.mjs`（确认写入带元数据）、`test/client-render.mjs`（分类/置信度/历史渲染）、Gherkin 场景 15                                                                                                                                                                                                                                                                                                                       |
| R15 | **渐进式更新 + 智能注入（issue #78 阶段 3）**：同主题多次出现 → 置信度提升/内容更新；矛盾标记待确认；长期未用降权；注入按「相关性 + 时效性 + 置信度」动态选择（替代简单 top-N）                                                 | `lib/memory-scoring.js` 纯函数：`mergeCandidate`（同主题判定 category + 归一文本包含/子序列 → 新增/置信度+1/内容更新/矛盾标记；跨明确分类不坍缩）、`decayConfidence`（default 90 天未用降权，下限 1）、`scoreForInjection`/`pickForInjection`（相关性：上下文关键词命中占比 + 时效性：exp 时间衰减 halfLife 7 天 + 置信度：归一化因子；默认权重 0.5/0.3/0.2）；`lib/prompt.js` 注册 section 改用评分选择（先 decay 再 pick，非简单 top-N）；确认写入走 `store.mergeAdd`（渐进合并）                                                                                                                                                                                                                                                                                                                                                                                          | `test/memory-scoring.mjs`（同主题/合并 add-reinforce-conflict/降权/评分/pick 排序）、`test/prompt.mjs`（智能注入与截断配合）、`test/candidates.mjs`（跨会话确认提升置信度）、Gherkin 场景 13-14                                                                                                                                                                                                                                                                                                                                                                          |

| R16 | **权限模式感知的保存确认（issue #208）**：`danger-full-access`（会话 approval policy = `never`）下 `memory_save` 不再被宿主自动拒绝，失败也可诊断 | 新增 `saveApproval: 'auto' \| 'always' \| 'never'`（默认 `auto`）；`lib/save-policy.js` 的 `createMemorySaveGate` 按策略 × 会话 approval policy 决策：`auto`+`never` → 免确认放行（条目仍标记来源、面板可见可撤销）、`auto`+`ask` → 原生确认（行为不变）、`always`+`never` → `{ kind: 'deny' }` + 可操作中文提示（不再笼统 rejected）、`never` → 从不确认；策略判定优先宿主 `ApprovalService.effectivePolicy(session)`（与宿主 `decide()` 同一函数，`dsh-user-approval/lib/index.js:155-178`），回落会话日志 `approval/policy` 折返（`exec.agent.session` 的 `seq` + `eventAt`，与宿主 `overrideOf` 同构），两者都判不出时保守按 `ask`（不误放行） | `test/save-policy.mjs`（策略判定 + 3×3 决策矩阵）、`test/tool.mjs`（门的四种组合行为）、Gherkin 场景 15-17；真实环境（隔离实例 + 真实模型 + 真实 GUI）：danger-full-access 无任何交互写入成功、workspace-write 弹窗且拒绝不写入、`always`+`never` 返回可操作提示 |
| R17 | **保存来源可追溯（issue #209）**：`memory_save` 写入的条目记录来源会话与时刻，面板与 `memory_query` 可见，旧数据不崩 | `executeSave` 把会话 id（`exec.agent.id`，宿主 `dsh-agent` 保证 `agent.id === agent.session.id`）与写入时刻传入 `store.add({ desc, source }, at)`；`renderQueryResult` 追加「（来源：会话 <前缀>）」；client `MetadataRow` 对 `source.sessionId` 非空条目渲染「agent 保存 · <前缀>」徽章；空来源（旧数据 / 面板手动新增）不加后缀、不渲染徽章 | `test/tool.mjs`（source 非空 / 旧数据可加载查询渲染 / query 渲染含前缀）、`test/client-render.mjs`（来源徽章只对带来源条目渲染）、Gherkin 场景 15；真实环境落盘条目 `source.sessionId` 非空 |
| R18 | **工具面记忆类型 + 删除工具（issue #192）**：agent 能**按类型**保存记忆、查询结果**显示类型**、并能**调用工具删除**指定记忆（删除需用户确认、支持全局/项目定位、不可撤销） | ① `memory_save` 新增可选 `category`（偏好/事实/项目/技术栈/工作流 5 类，enum 与中文标签同源 `lib/memory-scoring.js` 的 `CATEGORIES`/`categoryLabelOf`/`normalizeCategory`，不传或非法值默认 `fact`），`executeSave` 传 `{ desc, category, source }` 给 `store.add`；② `memory_query` 渲染行形如 `- [mem-…]〔偏好〕用 pnpm 装依赖（来源：会话 …）`（空结果文案不变），输出 schema 沿用 #191 的 `MEMORY_ITEM_SCHEMA`；③ 新增 `memory_delete` 工具（`src/delete-tool.ts`：`id` 必填、`scope`/`cwd` 与 save 同语义，默认 global）——**复用同一道 `tools/pre-execute` 确认门与同一套 `saveApproval` 权限模式策略**（#208），ask 文案带待删内容摘要（`lookupTarget` → `lookupDeleteTarget` 查真实 store；查不到仍询问、绝不静默放行）、未确认/被拒绝不删除、删除不存在的 id 明确报错、删除留 itemId + 调用会话的审计日志、输出把被删条目回执给模型；`src/write-tools.ts` 装配两个写工具与门（为守住 400 行文件门禁从 `index.ts` 拆出） | `test/tool.mjs`（category 参数/默认值/schema 同源/渲染标签/空场景/端到端）、`test/tool-delete.mjs`（工具 schema/删除语义/范围定位/不存在 id/拒绝路径/策略矩阵/审计日志）、`test/write-tools.mjs`（注册与门装配，变异验证有效）、`test/tool-output-schema.mjs`、Gherkin 场景 18-22；真实环境（隔离实例 + 真实模型 + 真实 GUI）走通「带 category 保存后落盘分类正确」与「delete 弹确认 → 批准后条目消失 / 拒绝后条目仍在」 |

## 2. 用户明确强调过的点

- **纯官方依赖**（issue #22 精神）：client 面板挂在官方 `slots` 扩展点（`settings.plugins.tab`），不依赖 dsh-better-sidebar；server 端只用官方 `ctx.systemPrompt` / `ctx.tools` / `webServer` / `webRuntime` / `sessions` 服务；工具定义直接构造（不导入 `@deepseek-ai/dsh-tools`——本仓库插件只解析 Node 内置模块与相对路径）。
- **记忆绝不静默变更**：所有写操作（新增/修改/删除/工具保存/**自动提取候选的确认与拒弃**）必须经用户确认。面板经 `POST /my-memory/api/memory` 强制 `confirmed: true`；`memory_save` 工具经 `tools/pre-execute` 确认门（`{ kind: 'ask' }` 触发 DSH 原生审批）；自动提取（issue #78）只产生「待确认候选」，确认/拒弃经 `POST /my-memory/api/candidates/{confirm,dismiss}` 且强制 `confirmed: true`——确认前绝不写入。
- **注入有上限**：全局记忆注入系统提示词受条数（`maxItems`）与单条长度（`maxDescLength`）双重上限，避免提示词膨胀。
- **自动提取默认关（issue #78）**：`autoLearn` 默认 `false`（用户显式开启才自动提取；提取不影响对话流程、不静默写入）；`extractor` 默认 `rule`（确定性规则提取器，CI/离线可测），`llm` 为预留占位。

## 需求变更记录

- **2026-09-12**：新增 R18（issue #192）——工具面能力补全：`memory_save` 支持可选 `category`（按类型保存）、`memory_query` 输出与渲染展示分类、新增 `memory_delete` 工具（复用同一道确认门 + `saveApproval` 策略，删除不存在的 id 明确报错）；同时**修正 R14 的文档漂移**——原文称 `POST /my-memory/api/memory` 的 add 支持可选 meta（category/source），实际 `applyAdd` 只传 desc（分类恒为 `fact`、来源为空），R14 已改为与实现一致的描述。
- **2026-09-12**：新增 R16/R17（issue #208 / #209）——登记 #191 端到端验证（隔离实例 + 真实模型 + 真实 GUI）发现的遗留缺口：① 用户当前 `permission.defaultPreset: danger-full-access` → 会话 approval policy = `never` → 宿主对 `{ kind: 'ask' }` 自动 rejected（`dsh-user-approval/lib/index.js:178`），导致 R10 的确认门在该模式下失效（`memory_save` 100% 失败且无弹窗）；② 落盘条目 `source` 恒为 `{ sessionId: '', at: 0 }`，R14 声明的来源字段没被填值。决策（用户 2026-09-12「全由我们自己的插件进行处理」）：宿主能力缺口一律插件侧接管、不等上游；`auto` 模式下「记忆绝不静默变更」调整为「可见、可追溯、可撤销的写入」（该模式是用户主动选择的全权委托，理由见插件 README「权限模式与保存确认」）。

- **2026-09-12**：修复 R5/R10 的工具输出契约（issue #191）——`memory_save`/`memory_query` 的 `output.schema` 此前只声明 `id/desc/createdAt/updatedAt`，而 store 真实条目恒定带 `category/source/confidence/relatedIds/history/status`，宿主对每个成功值跑 `validateJsonSchemaValue`（`additionalProperties: false`）→ 保存必抛 `ToolOutputError`（query 在记忆库非空时同样；空记忆库掩盖了它）。现由共享常量 `MEMORY_ITEM_SCHEMA`（`src/tool.ts` 导出，#192 及后续工具可复用）生成 `QUERY_OUTPUT`/`SAVE_OUTPUT`；新增 `test/tool-output-schema.mjs`，用真实 store 返回值（save / query / project / 合并元数据 / 非空 items）跑与宿主校验器语义等价的严格校验，并断言字段集双向一致，防再次漂移。

- **2026-09-07**：新增 R13-R15（issue #78 渐进式索引记忆）——会话结束自动提取记忆候选（`autoLearn` 默认关、`extractor: rule|llm` 占位）进入待确认列表；条目带分类/来源/置信度/更新时间/演进历史元数据（旧数据兼容回退）；同主题渐进合并（置信度提升/内容更新/矛盾标记）、长期未用降权、智能注入（相关性+时效性+置信度评分替代简单 top-N）；候选独立存 `$DSH_HOME/memory/candidates.json`，正式记忆只含用户确认过的条目。
- **2026-09-02**：R3 增补记忆面板视觉设计（issue #110）：条目卡片化、长条目截断+展开、徽标分类+数量、操作图标组与内容分离、更新时间相对化+排序、空/加载/错误状态视觉统一。
- **2026-09-03**：v0.1.0 建立本清单（issue #38 落地）。
- **2026-09-03**：R3 面板增加「打开即自动加载当前会话项目记忆」（issue #104），新增 `GET /my-memory/api/session`（解析会话 cwd）。
- **2026-09-05**：新增 R10（issue #107）——`memory_save` 工具 + `tools/pre-execute` 用户确认门（DSH 原生审批），`proactivePropose` 配置预留（默认关，#78 阶段）。
- **2026-09-06**：新增 R11（issue #108）——项目记忆存储从 `<项目根>/.dsh/memory.json` 迁移到 `$DSH_HOME/memory/projects/<项目 id>.json` 集中位置，旧数据首次访问自动迁移不丢失，项目目录不再产生 `.dsh/`；R1 现状同步更新。
- **2026-09-04**：新增 R12（issue #105）——记忆内容精简：保存时引导浓缩（`maxEntryLength` 默认 50，输入超长提示 + 确认面板概要预览）、面板概要/详情两级展示（列表显示首句概要，点击展开完整详情，存储保留完整 desc）、注入按语义截断（优先概要/首句，不截断句子中间）；共享纯函数 `lib/memory-text.js` 为 #78 自动提取预留；R2 现状同步语义截断说明。
