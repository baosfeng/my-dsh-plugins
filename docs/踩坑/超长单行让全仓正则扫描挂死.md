---
title: 超长单行让全仓正则扫描挂死
description: 对"全仓所有文本行"跑正则的工具，撞上压缩产物（mermaid.min.js 单行 8.9MB）会触发 O(n²) 回溯，脚本挂死且无任何输出；修法是先做体积/行长方护再匹配
created: 2026-09-11
updated: 2026-09-11
---

# 超长单行让全仓正则扫描挂死

## 现象

新写的文档引用完整性校验脚本 `scripts/check-links.mjs`（扫描全仓 markdown 与文本行，校验链接、锚点、路径 token）首次接入时**挂死**：

```bash
$ node scripts/check-links.mjs
# 180 秒无任何输出（被外部超时杀掉）
```

没有报错、没有进度、没有 CPU 打满的明显信号——看起来像"卡在某个文件上"，但不知道是哪个。

## 根因

匹配 markdown 链接的正则被跑在**每一行**上，而仓库里有压缩产物：

```
plugins/dsh-mermaid-render/vendor/mermaid.min.js   ← 单行 8.9 MB
```

对一条 8.9 MB 的行跑带多分支的链接正则（`!?\[[^\]]*\]\([^)]*\)` 这类），回溯成本随行长**平方级**增长 → 单行就耗掉几分钟到无限期。全仓 800+ 文件中只要有一行这种"压缩成一行"的产物，整个脚本就废了。

同类文件还有 `*-lib.js`、`*.min.js`、打包产物等，它们本来就永远不会包含需要校验的相对链接。

## 修法

在**匹配之前**做三重防护（顺序即成本从低到高）：

| 防护                  | 阈值                                   | 理由                             |
| --------------------- | -------------------------------------- | -------------------------------- |
| 排除压缩/打包产物路径 | `vendor/**`、`*.min.js`、`*-lib.js` 等 | 这类文件不承载被校验的引用       |
| 整体跳过超大文件      | > 1 MB                                 | 文档类文件不会有这个量级         |
| 跳过超长单行          | > 100 000 字符单行                     | 保留"大文件里混着正常文本"的场景 |

并配两条回归测试（构造超长单行 fixture 断言脚本仍在秒级完成），避免以后有人"优化"掉防护。

修复后同一仓库实测 **0.18 秒**扫完 876 个文件。

## 通用教训

**任何"扫全仓所有文本"的工具（链接校验、拼写检查、TODO 扫描、密钥扫描、正则重写），第一件事都是防压缩产物**——它们的特征是"单行超长"，而不是"文件大"。只按文件大小设限会漏（一个 500 KB 的 min.js 同样能让正则跪）；只按行数设限也会漏。**必须同时看单行长度。**

## 相关

- `scripts/check-links.mjs`（含三重防护与注释）
- `scripts/test/check-links.test.mjs`（含超长单行回归用例）
- 本坑是 2026-09-11 全仓引用审计（37 处失效引用）的后续：审计发现"没有门禁"，新建门禁时踩到本坑
