# language: zh-CN
# 需求来源：GitHub issue + docs/文件活动追踪/概述.md（R1-R6、R9、R12）
# 验收基准：host-smoke.mjs / host-edge.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: 文件活动追踪
  作为使用 DSH 侧边栏的用户
  我想要按会话查看最近访问的文件和统计
  以便快速回到刚才工作过的文件

  场景大纲: 读取文件后进入最近访问并计数
    假如 会话 "<会话>" 已就绪
    当 代理读取了文件 "<路径>"
    那么 最近访问列表包含 "<路径>"
    并且 该文件 "<路径>" 的读取计数为 <次数>

    例子:
      | 会话  | 路径          | 次数 |
      | s-001 | /work/a.txt   | 1    |
      | s-001 | /work/b.txt   | 1    |

  场景: 重复读取同一文件计数累加
    假如 会话 "s-010" 已就绪
    当 代理读取了文件 "/work/a.txt"
    并且 代理再次读取了文件 "/work/a.txt"
    那么 该文件 "/work/a.txt" 的读取计数为 2

  场景: 首次写入判定为新增，再次写入判定为修改
    假如 会话 "s-002" 已就绪
    当 代理写入了文件 "/work/b.txt"
    那么 该文件 "/work/b.txt" 的新增计数为 1
    当 代理再次写入了文件 "/work/b.txt"
    那么 该文件 "/work/b.txt" 的修改计数为 1
    并且 最近访问列表中 "/work/b.txt" 只出现一次

  场景: 编辑操作判定为修改
    假如 会话 "s-003" 已就绪
    当 代理编辑了文件 "/work/a.txt"
    那么 该文件 "/work/a.txt" 的修改计数为 1

  场景: 读取不存在的文件不产生记录
    假如 会话 "s-004" 已就绪
    当 代理尝试读取缺失的文件 "/work/missing.txt"
    那么 会话 "s-004" 的统计中不存在 "/work/missing.txt"

  场景: 最近访问列表有上限且最新优先
    假如 会话 "s-005" 已就绪
    当 代理读取了 12 个不同的文件
    那么 最近访问列表长度为 5
    并且 最近访问列表第一项是最新读取的文件

  场景: 会话之间数据隔离
    假如 会话 "s-006" 已就绪
    并且 代理读取了文件 "/work/a.txt"
    当 查询会话 "s-other" 的统计
    那么 会话 "s-other" 的统计为空

  场景: 重启后数据完整恢复
    假如 会话 "s-007" 已就绪
    并且 代理读取了文件 "/work/a.txt"
    并且 等待持久化完成
    当 插件实例重启
    那么 会话 "s-007" 的统计包含 "/work/a.txt"
    并且 该文件 "/work/a.txt" 的读取计数为 1

  场景: 跨站请求被拒绝
    假如 会话 "s-008" 已就绪
    当 用非回环 host 请求统计接口
    那么 响应状态码为 403

  场景: 清空会话记录
    假如 会话 "s-009" 已就绪
    并且 代理读取了文件 "/work/a.txt"
    当 清空会话 "s-009" 的记录
    那么 会话 "s-009" 的统计为空

  场景: 工作区外已记录文本经文本路由可预览（issue #68）
    假如 会话 "s-020" 已就绪
    当 代理读取了文件 "/tmp/outside-notes.md"
    并且 创建文本文件 "/tmp/outside-notes.md" 内容为 "# 笔记"
    并且 请求该文件 "/tmp/outside-notes.md" 的文本路由
    那么 文本路由返回内容 "# 笔记"
    并且 响应状态码为 200

  场景: 已记录但已删除的文件经文本路由返回 404（issue #68）
    假如 会话 "s-021" 已就绪
    当 代理读取了文件 "/tmp/deleted-notes.md"
    并且 请求该文件 "/tmp/deleted-notes.md" 的文本路由
    那么 响应状态码为 404
