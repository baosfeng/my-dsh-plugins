# language: zh-CN
# 需求来源：docs/md渲染/需求清单.md
# 验收基准：table-detect.mjs / client-render.mjs / markdown-view.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: Markdown 表格渲染增强
  作为使用 DSH 对话的用户
  我想要非思考模式下模型输出的表格（含不标准格式）渲染为表格
  以便直接查看表格数据而无需阅读原始 markdown 语法

  场景: 不标准表格（无首尾管道符）渲染为表格
    假如 渲染插件已启动且对话含不标准表格段落
    那么 段落被替换为表格元素
    并且 表格包含表头与数据行
    并且 表格外层有横向滚动容器

  场景: 标准表格已渲染时不被重复处理
    假如 渲染插件已启动且对话含已渲染的表格
    那么 已渲染的表格保持原样

  场景: 非表格文本不受影响
    假如 渲染插件已启动且对话含普通文本段落
    那么 普通文本段落保持原样

  场景: 插件激活时注入表格样式
    假如 渲染插件已启动且对话含不标准表格段落
    那么 页面注入包含表格规则的样式

  场景: 思考模式（reasoning 块）表格渲染不受影响
    假如 渲染插件已启动且对话含思考块内已渲染的表格
    那么 思考块内的表格保持原样

  场景: 统一渲染器渲染标准表格
    假如 统一渲染器已加载
    当 渲染含分隔行的文本块
    那么 输出包含 table 标签
    并且 输出包含表头与数据行

  场景: 统一渲染器输出代码块容器
    假如 统一渲染器已加载
    当 渲染含 mermaid 围栏的文本块
    那么 输出包含 md-code-block 容器
    并且 代码块保留语言标记

  场景: 统一渲染器渲染公式
      假如 统一渲染器已加载
      当 渲染含行内公式的文本块
      那么 输出包含公式元素

    场景: 分数命令渲染为分数结构（issue #82）
      假如 统一渲染器已加载
      当 渲染含分数命令的文本块
      那么 输出包含分数结构

    场景: 根号命令渲染为根号结构（issue #82）
      假如 统一渲染器已加载
      当 渲染含根号命令的文本块
      那么 输出包含根号结构

    场景: 上下标渲染为脚本结构（issue #82）
      假如 统一渲染器已加载
      当 渲染含上下标命令的文本块
      那么 输出包含上下标结构

    场景: 求和积分渲染为大符号结构（issue #82）
      假如 统一渲染器已加载
      当 渲染含求和命令的文本块
      那么 输出包含大符号结构

    场景: 希腊字母命令渲染为符号（issue #82）
      假如 统一渲染器已加载
      当 渲染含希腊字母命令的文本块
      那么 输出包含希腊字母符号

    场景: 无法解析的公式保持原文（issue #82）
      假如 统一渲染器已加载
      当 渲染含无法解析公式的文本块
      那么 输出不包含分数结构
      并且 无法解析的公式原文保留

  场景: 统一渲染器对异常公式给出错误提示
    假如 统一渲染器已加载
    当 渲染含未闭合公式的文本块
    那么 输出包含公式错误标记
    并且 原始公式文本保留

  场景: 统一渲染器对空公式给出错误提示
    假如 统一渲染器已加载
    当 渲染含空公式的文本块
    那么 输出包含公式错误标记

  场景: 统一渲染器输出复制按钮（issue #74）
    假如 统一渲染器已加载
    当 渲染含 mermaid 围栏的文本块
    那么 输出包含复制按钮

  场景: 长表格默认折叠且可展开（issue #83）
    假如 渲染插件已启动且对话含超过 20 行的表格段落
    那么 表格默认只显示前 20 行
    并且 表格下方有展开按钮
    当 点击展开按钮
    那么 表格显示全部行

  场景: 点击表头按列排序（issue #83）
    假如 渲染插件已启动且对话含超过 20 行的表格段落
    当 点击表头
    那么 表格行按该列排序
    并且 表头显示排序指示

  场景: 关闭语法高亮后代码块无 token 高亮（issue #84）
    假如 渲染插件已启动且渲染开关 "syntaxHighlight=false"
    当 渲染含 js 代码块的文本块
    那么 输出不包含语法高亮 token span
    并且 语言标签仍渲染

  场景: 关闭复制按钮后无复制按钮（issue #84）
    假如 渲染插件已启动且渲染开关 "copyButton=false"
    当 渲染含 js 代码块的文本块
    那么 输出不包含复制按钮

  场景: 关闭任务列表后无 checkbox（issue #84）
    假如 渲染插件已启动且渲染开关 "taskList=false"
    当 渲染含任务列表标记的文本块
    那么 输出不包含 checkbox
    并且 任务标记文本保留

  场景: 关闭公式结构后公式不渲染（issue #84）
    假如 渲染插件已启动且渲染开关 "mathStructures=false"
    当 渲染含行内公式的文本块
    那么 输出不包含公式元素
    并且 公式原文保留

  场景: 关闭长表格折叠后全部行可见（issue #84）
    假如 渲染插件已启动且渲染开关 "tableFold=false" 且对话含超过 20 行的表格段落
    那么 表格不渲染折叠行
    并且 表格不渲染展开按钮

  场景: 代码块复制按钮默认位于右下角（issue #146）
     假如 统一渲染器已加载
     当 渲染含 js 代码块的文本块
     那么 代码块复制按钮位于代码块右下角
     并且 代码块复制按钮不在头部

  场景: 配置代码主题后代码块应用主题（issue #146）
     假如 渲染插件已启动且渲染开关 "codeTheme=one-dark"
     当 渲染含 js 代码块的文本块
     那么 代码块携带对应主题标记 data-theme="one-dark"
     并且 输出包含语法高亮 token span

  场景: 复制按钮位置切换为头部（issue #146）
     假如 渲染插件已启动且渲染开关 "copyButtonPosition=header"
     当 渲染含 js 代码块的文本块
     那么 代码块复制按钮位于头部
     并且 输出包含复制按钮

  场景: 关闭语法高亮后代码块不受主题影响（issue #146）
     假如 渲染插件已启动且渲染开关 "syntaxHighlight=false"
     当 渲染含 js 代码块的文本块
     那么 输出不包含语法高亮 token span
     并且 代码块不携带主题标记
