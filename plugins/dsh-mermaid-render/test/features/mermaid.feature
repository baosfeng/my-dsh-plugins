# language: zh-CN
# 需求来源：docs/mermaid渲染/需求清单.md
# 验收基准：client-render.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: Mermaid 渲染
  作为使用 DSH 对话的用户
  我想要 mermaid 代码块渲染为图表卡片
  以便直接查看图表而无需离开页面

  场景: mermaid 代码块渲染为图表卡片
    假如 渲染插件已启动且页面含 mermaid 与普通代码块
    那么 生成一个图表卡片
    并且 卡片包含渲染中的加载状态
    并且 卡片提供预览与代码切换
    并且 原始代码块被隐藏

  场景: 非 mermaid 代码块不被渲染
    假如 渲染插件已启动且页面只含普通代码块
    那么 不生成任何图表卡片

  场景: 插件激活时注入卡片样式
    假如 渲染插件已启动且页面含 mermaid 与普通代码块
    那么 页面注入包含卡片规则的样式

  场景: 图表卡片提供导出按钮（issue #85）
    假如 渲染插件已启动且页面含 mermaid 与普通代码块
    那么 卡片提供下载 PNG 与下载 SVG 按钮
    并且 卡片提供复制代码按钮

  场景: 复制源码调用剪贴板（issue #85）
    假如 渲染插件已启动且页面含 mermaid 与普通代码块
    当 用户点击复制代码按钮
    那么 剪贴板写入 mermaid 源码

  场景: 默认注入 mermaid 能力说明到系统提示词（issue #194）
    假如 渲染插件 host 半以默认配置挂载
    那么 系统提示词注册了 mermaid 能力说明段
    并且 说明文本含围栏语言标识 mermaid 与七类图表关键字
    并且 说明文本长度不超过 500 字符

  场景: 关闭开关后不再注入（issue #194）
    假如 渲染插件 host 半以注入开关关闭的方式挂载
    那么 系统提示词未注册任何说明段
    并且 插件仍记录挂载日志
