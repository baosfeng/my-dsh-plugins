# language: zh-CN
# 需求来源：GitHub issue + docs/上下文透镜/概述.md（需求 1-6）
# 验收基准：host-events.mjs / host-budget.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: 上下文透镜与成本治理
  作为 DSH 用户
  我想要看到每次请求的上下文占用与 token 用量
  以便控制长会话的上下文与成本

  场景: 请求头被统计为系统与工具构成
    假如 上下文透镜插件已启动
    当 会话 "s-1" 的请求头包含系统提示与工具
    那么 会话 "s-1" 的系统构成大于 0
    并且 会话 "s-1" 的工具构成大于 0

  场景: 模型请求记录真实 token 用量
    假如 上下文透镜插件已启动
    当 会话 "s-1" 的模型返回带用量统计的回复
    那么 会话 "s-1" 的输入 token 等于 100
    并且 会话 "s-1" 的请求数等于 1
    并且 请求的 prompt token 等于 130

  场景: 注入消息计入注入构成而非用户构成
    假如 上下文透镜插件已启动
    当 会话 "s-1" 收到来自插件的注入消息
    那么 会话 "s-1" 的注入构成大于 0
    并且 会话 "s-1" 的用户构成等于 0

  场景: 会话之间互相隔离
    假如 上下文透镜插件已启动
    当 会话 "s-1" 的模型返回带用量统计的回复
    并且 会话 "s-2" 的模型返回带用量统计的回复
    那么 会话 "s-1" 的输入 token 等于 100
    并且 会话 "s-2" 的输入 token 等于 200

  场景: 统计在重启后恢复
    假如 上下文透镜插件已启动
    当 会话 "s-1" 的模型返回带用量统计的回复
    并且 插件重启
    那么 会话 "s-1" 的输入 token 等于 100

  场景: 每轮预算超限时提醒
    假如 上下文透镜插件已启动且每轮预算为 10
    当 会话 "s-1" 的模型返回带用量统计的回复
    并且 代理 "s-1" 准备执行下一步
    那么 产生 1 条预算告警
    并且 下一步未被拦截

  场景: 每轮预算超限时拦截
    假如 上下文透镜插件已启动且每轮预算为 10 且模式为拦截
    当 会话 "s-1" 的模型返回带用量统计的回复
    并且 代理 "s-1" 准备执行下一步
    那么 产生 1 条预算告警
    并且 下一步被拦截

  场景: 预算配置可动态更新
    假如 上下文透镜插件已启动
    当 通过接口更新预算为每轮 50 且模式为拦截
    那么 状态接口返回的预算配置为每轮 50 且模式为拦截

  场景: 上下文占用接近窗口时产生溢出预警
    假如 上下文透镜插件已启动
    当 会话 "s-1" 的上下文窗口为 100
    并且 会话 "s-1" 的模型返回 80 输入 token 的回复
    并且 代理 "s-1" 准备执行下一步
    那么 产生 1 条溢出预警
    并且 溢出预警级别为 "warn"

  场景: 溢出预警阈值可按配置生效
    假如 上下文透镜插件已启动
    当 通过接口把溢出预警阈值设为预警 50 告警 80
    并且 会话 "s-1" 的上下文窗口为 100
    并且 会话 "s-1" 的模型返回 60 输入 token 的回复
    并且 代理 "s-1" 准备执行下一步
    那么 产生 1 条溢出预警
    并且 溢出预警级别为 "warn"
