# language: zh-CN
# 需求来源：docs/插件治理/需求清单.md（R1-R5、R7、R11、R14、R15）
# 验收基准：host-smoke.mjs / host-edge.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: 插件治理
  作为 DSH 用户
  我想要新插件在隔离候选区安全加载
  以便坏插件不会拖垮整个环境

  场景大纲: 候选条目挂载成功并转正
    假如 候选区已写入条目 "<id>" 名为 "<名字>"
    当 守护进程启动
    那么 条目 "<id>" 已被挂载
    并且 条目 "<id>" 已转正进入 promoted 清单
    并且 候选区文件不再包含 "<id>"

    例子:
      | id           | 名字      |
      | nice-plugin  | 正常插件  |
      | second-plugin | 另一插件  |

  场景: 挂载失败的条目被隔离并记录失败原因
    假如 候选区已写入条目 "bad-plugin" 名为 "坏插件"
    并且 loader 挂载 "bad-plugin" 会失败
    当 守护进程启动
    那么 条目 "bad-plugin" 未被挂载
    并且 条目 "bad-plugin" 的失败次数为 1
    并且 候选区文件仍包含 "bad-plugin"
    并且 状态记录包含失败原因

  场景: 连续失败后冻结不再自动重试
    假如 候选区已写入条目 "flaky" 名为 "抖动插件"
    并且 loader 挂载 "flaky" 会失败
    当 守护进程连续启动 3 次
    那么 条目 "flaky" 处于冻结状态

  场景: 安全模式跳过所有候选
    假如 候选区已写入条目 "good-plugin" 名为 "正常插件"
    并且 安全模式已开启
    当 守护进程启动
    那么 没有任何条目被挂载

  场景: 重启后转正的条目恢复挂载
    假如 状态文件已记录转正条目 "old-1"
    当 守护进程启动
    那么 条目 "old-1" 已被挂载

  场景: id 冲突的候选被拒绝
    假如 候选区已写入条目 "conflict" 名为 "冲突插件"
    并且 loader 已有条目 "conflict"
    当 守护进程启动
    那么 条目 "conflict" 未被挂载
    并且 状态记录包含冲突提示

  场景: 跨站 API 请求被拒绝
    假如 守护进程启动
    当 用非回环 host 请求状态接口
    那么 响应状态码为 403

  场景: 依赖缺失的候选被预检拦截并标记依赖缺失
    假如 候选区已写入条目 "dep-miss" 名为 "dsh-dep-miss"
    并且 候选插件 "dsh-dep-miss" 声明缺失的 peer 依赖 "dsh-shared"
    当 守护进程启动
    那么 条目 "dep-miss" 未被挂载
    并且 条目 "dep-miss" 处于依赖缺失失败
    并且 状态记录包含依赖安装建议

  场景: 依赖就绪的候选正常挂载并转正
    假如 候选区已写入条目 "dep-ok" 名为 "dsh-dep-ok"
    并且 候选插件 "dsh-dep-ok" 的 peer 依赖 "react" 已安装版本 "18.3.1"
    当 守护进程启动
    那么 条目 "dep-ok" 已被挂载
    并且 条目 "dep-ok" 已转正进入 promoted 清单

  场景: 代码错误的候选被标记为代码错误
    假如 候选区已写入条目 "bad-plugin" 名为 "dsh-code"
    并且 loader 挂载 "bad-plugin" 会失败
    当 守护进程启动
    那么 条目 "bad-plugin" 处于代码错误失败
    并且 状态记录包含失败原因

  场景: 启动名册存在问题的条目被预检记录且不阻断启动
    假如 loader 名册已有条目 "needy" 名为 "dsh-needy"
    并且 名册插件 "dsh-needy" 声明缺失的 peer 依赖 "dsh-shared"
    当 守护进程启动
    那么 预检报告写入启动区问题
    并且 状态记录包含启动区问题事件
