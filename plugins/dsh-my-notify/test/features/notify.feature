# language: zh-CN
# 需求来源：GitHub issue + docs/通知提醒/概述.md（需求 1-9）
# 验收基准：host-smoke.mjs / host-edge.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: 通知提醒
  作为 DSH 用户
  我想要在会话结束、询问和审批时收到通知
  以便不盯着页面也能及时响应

  场景: 会话空闲时推送结束通知
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 顶层代理 "s-1" 变为空闲
    那么 客户端收到一条通知
    并且 通知类型为 "end"
    并且 通知携带会话标题

  场景: 子代理空闲不推送
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 子代理 "sub-1" 变为空闲
    那么 客户端未收到任何通知

  场景: 无标记子代理空闲不推送
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 无标记子代理 "sub-2" 变为空闲
    那么 客户端未收到任何通知

  场景: 开启 subagentEnd 后子代理空闲推送并标记
    假如 通知插件已启动且开启了 subagentEnd
    并且 有客户端订阅了实时通道
    当 无标记子代理 "sub-3" 变为空闲
    那么 客户端收到一条 "end" 通知
    并且 通知标记为 "subagent"
    并且 通知标题以 "子代理" 开头

  场景: 同会话去重窗口内只推一次
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 顶层代理 "dup-1" 连续空闲 2 次
    那么 客户端只收到 1 条通知

  场景: 询问工具触发通知且流程继续
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 代理调用 ask_user_question 工具
    那么 客户端收到一条 "ask" 通知
    并且 工具流程的 next() 被调用

  场景: 非询问工具不触发
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 代理调用 bash 工具
    那么 客户端未收到任何通知
    并且 工具流程的 next() 被调用

  场景: 审批请求触发通知
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 代理发起审批请求
    那么 客户端收到一条 "approval" 通知
    并且 通知携带工具名
    并且 审批流程的 next() 被调用

  场景: 配置可关闭某类触发
    假如 通知插件已启动且配置关闭了 end/ask/approval
    并且 有客户端订阅了实时通道
    当 顶层代理 "s-off" 变为空闲
    并且 代理调用 ask_user_question 工具
    并且 代理发起审批请求
    那么 客户端未收到任何通知

  场景: 远程 hook 本机触发成功
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 本机 POST 触发接口携带标题 "CI 完成"
    那么 响应状态码为 200
    并且 客户端收到一条 "remote" 通知
    并且 通知标题为 "CI 完成"

  场景: 远程 hook 错误 token 被拒绝
    假如 通知插件已启动且配置了 apiToken
    当 用错误 token 调用触发接口
    那么 响应状态码为 403

  场景: SSE 客户端断开后不再接收广播
    假如 通知插件已启动
    并且 有客户端订阅了实时通道
    当 该客户端断开连接
    并且 顶层代理 "gone-1" 变为空闲
    那么 该客户端未收到任何通知
