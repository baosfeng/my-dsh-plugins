# language: zh-CN
# 需求来源：GitHub issue + docs/可观测性/概述.md（需求 6-8）
# 验收基准：host-git.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: 结构化 Git 工具
  作为 DSH 用户
  我想要进行类型化提交（Conventional Commits）
  以便提交历史清晰可追溯

  场景: 生成类型化提交消息
    假如 可观测性插件已启动
    并且 存在一个临时 git 仓库且已修改文件
    当 请求提交类型 "feat" 范围 "panel" 描述 "add timeline"
    那么 响应状态码为 200
    并且 提交消息为 "feat(panel): add timeline"

  场景: 非法提交类型被拒绝
    假如 可观测性插件已启动
    并且 存在一个临时 git 仓库
    当 请求提交类型 "nope" 描述 "x"
    那么 响应状态码为 400

  场景: 空描述被拒绝
    假如 可观测性插件已启动
    并且 存在一个临时 git 仓库
    当 请求提交类型 "feat" 描述 ""
    那么 响应状态码为 400

  场景: 查询仓库状态
    假如 可观测性插件已启动
    并且 存在一个临时 git 仓库且已修改文件
    当 查询该仓库的状态
    那么 响应状态码为 200
    并且 状态包含 1 个未暂存变更

  场景: 对真实仓库执行类型化提交
    假如 可观测性插件已启动
    并且 存在一个临时 git 仓库且已修改文件
    当 对该仓库请求提交类型 "fix" 描述 "repair the widget"
    那么 响应状态码为 200
    并且 提交返回 hash
    并且 仓库最近提交消息为 "fix: repair the widget"
