# language: zh-CN
# 需求来源：GitHub issue + docs/可观测性/概述.md（需求 1-5）
# 验收基准：host-audit.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: 事件审计与轨迹数据
  作为 DSH 用户
  我想要 agent 的行为被记录下来并可查询
  以便追溯会话中发生了什么

  场景: agent 状态变化被记录
    假如 可观测性插件已启动
    当 顶层代理 "s-1" 的状态变为 "running"
    那么 会话 "s-1" 的事件数等于 1
    并且 第一个事件的类型为 "agent_status"

  场景: 模型流被记录为开始与结束
    假如 可观测性插件已启动
    当 代理 "s-2" 的模型流输出 2 个数据块
    那么 会话 "s-2" 中类型为 "llm_stream" 的事件数等于 2
    并且 模型流事件包含块数统计

  场景: 工具调用与结果被记录
    假如 可观测性插件已启动
    当 代理 "s-3" 调用 bash 工具并成功返回
    那么 会话 "s-3" 中类型为 "tool_call" 的事件数等于 1
    并且 会话 "s-3" 中类型为 "tool_result" 的事件数等于 1
    并且 工具结果事件标记为成功

  场景: 会话之间互相隔离
    假如 可观测性插件已启动
    当 代理 "s-4" 的状态变为 "running"
    并且 代理 "s-5" 的状态变为 "idle"
    那么 会话 "s-4" 的事件数等于 1
    并且 会话 "s-5" 的事件数等于 1
    并且 会话列表包含 2 个会话

  场景: 事件在重启后恢复
    假如 可观测性插件已启动
    当 代理 "s-6" 的状态变为 "running"
    并且 插件重启
    那么 会话 "s-6" 的事件数等于 1

  场景: 按类型过滤查询
    假如 可观测性插件已启动
    当 代理 "s-7" 的状态变为 "idle"
    并且 代理 "s-7" 调用 bash 工具并成功返回
    那么 会话 "s-7" 中类型为 "tool_call" 的事件数等于 1
    并且 会话 "s-7" 中类型为 "agent_status" 的事件数等于 1

  场景: 高频事件下落盘为增量追加（写放大防护）
    假如 可观测性插件已启动
    当 代理 "amp" 持续产生 200 条状态事件
    并且 等待落盘稳定
    那么 审计数据文件以增量追加方式增长
    并且 落盘字节数不超过事件本体字节的 1.6 倍

  场景: 资源监控可查询
    假如 可观测性插件已启动
    当 请求资源采样
    那么 资源采样包含 CPU 与内存指标
    并且 资源采样包含告警列表
    并且 资源采样包含降级状态
