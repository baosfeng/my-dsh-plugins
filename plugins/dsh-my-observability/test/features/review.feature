# language: zh-CN
# 需求来源：GitHub issue + docs/可观测性/概述.md（需求 9-12）
# 验收基准：host-review.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: 增量 diff 审查
  作为 DSH 用户
  我想要在提交前审查增量 diff
  以便提前发现调试残留、密钥泄露等问题

  场景: 调试残留被标记
    假如 可观测性插件已启动
    并且 存在一个含调试语句变更的临时 git 仓库
    当 请求审查该仓库
    那么 响应状态码为 200
    并且 审查报告包含 "debug-statement" 问题

  场景: 密钥硬编码被标记为错误
    假如 可观测性插件已启动
    并且 存在一个含密钥硬编码变更的临时 git 仓库
    当 请求审查该仓库
    那么 审查报告包含 "secret-leak" 问题
    并且 该问题严重级别为 "error"

  场景: 干净的变更没有错误级问题
    假如 可观测性插件已启动
    并且 存在一个含干净变更的临时 git 仓库
    当 请求审查该仓库
    那么 审查报告的错误数为 0
    并且 审查报告的警告数为 0

  场景: 非 git 仓库被拒绝
    假如 可观测性插件已启动
    当 请求审查不存在的路径
    那么 响应状态码为 400

  场景: AI 审查增强合并到报告
    假如 可观测性插件已启动且 AI 审查服务可用
    并且 存在一个含干净变更的临时 git 仓库
    当 请求审查该仓库
    那么 审查报告包含 AI 结论
    并且 AI 结论的 verdict 为 "approve"
