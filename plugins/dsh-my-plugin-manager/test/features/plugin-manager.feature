# language: zh-CN
# 需求来源：issue #439（下线与官方重复的安装/卸载/启停/清单管理，只保留真增量）
# 验收基准：host-api.mjs / host-registry.mjs 的 Gherkin 化表达
功能: 插件市场面板
  作为 DSH 用户
  我想要在官方插件页之外补充市场搜索、更新检查与插件详情
  以便在安装之前先了解一个插件（安装 / 卸载 / 启停 / 清单由官方插件页承担）

  场景: 市场搜索返回官方与用户结果
    当 搜索关键词 "dsh" 返回官方与用户结果
    那么 搜索结果包含 "@deepseek-ai/dsh-base"
    并且 搜索结果包含 "dsh-a"

  场景: 插件详情返回 README、版本历史与对等依赖缺失
    当 请求插件详情 "dsh-a"
    那么 详情包含 README "hello readme"
    并且 详情版本历史包含 "1.0.0"
    并且 详情元数据包含许可证 "MIT"
    并且 详情对等依赖包含缺失 "dsh-shared"
    并且 详情对等依赖不缺失 "cordis"

  场景: 加载失败时插件详情返回失败
    当 加载不存在的插件详情 "ghost"
    那么 详情加载失败且给出错误消息

  场景: 已安装清单接口已下线
    当 请求已安装清单
    那么 响应状态码为 404

  场景: 安装接口已下线（官方插件页承担）
    当 请求安装接口
    那么 响应状态码为 404
    并且 响应错误说明路由未知

  场景: 跨站 API 请求被拒绝
    当 用非回环 host 搜索插件
    那么 响应状态码为 403
