# language: zh-CN
# 需求来源：issue #45（工程配置：dependabot/renovate + knip/jscpd 代码质量检测）
# 验收基准：fence.mjs / http.mjs 的 Gherkin 化表达；共享工具包行为与抽取前各插件实现一致

功能: 共享工具包（dsh-shared）
  作为 DSH 插件开发者
  我想要使用统一的信任围栏与 HTTP JSON 工具
  以便多插件共用实现、消除复制粘贴（issue #45）

  场景: loopback 请求放行
    假如 请求 host 为 "127.0.0.1:3080"
    那么 信任围栏判定为可信

  场景: 非 loopback 且无受信权威时拒绝
    假如 请求 host 为 "evil.example.com" 且无受信权威
    那么 信任围栏判定为不可信

  场景: 受信权威放行
    假如 请求 host 为 "dsh.local:3080" 且受信权威含 "dsh.local"
    那么 信任围栏判定为可信

  场景: cross-site 请求拒绝
    假如 请求 host 为 "127.0.0.1:3080" 且 sec-fetch-site 为 "cross-site"
    那么 信任围栏判定为不可信

  场景: 跨源 origin 拒绝
    假如 请求 host 为 "127.0.0.1:3080" 且 origin 为 "http://evil.example.com"
    那么 信任围栏判定为不可信

  场景: JSON 请求体读取
    假如 请求体为 "{\"a\":1}"
    当 读取 JSON 请求体
    那么 得到对象 "{\"a\":1}"

  场景: JSON 响应写入
    当 写入 JSON 响应 状态码 200 值 "{\"ok\":true}"
    那么 响应状态码为 200 且内容为 "{\"ok\":true}"

  场景: 错误响应写入
    当 写入错误响应 消息 "boom"
    那么 响应状态码为 400 且内容含错误消息 "boom"

  # issue #198：资源护栏默认安全（默认 1s 节流 + 1MB 字节上限，拦截不静默）
  场景: 原子写默认护栏拦截超频写并计数
    假如 已成功写入一次快照
    当 在默认节流窗口内再写一次
    那么 写入被拒绝且产生告警
    而且 护栏节流计数递增 1

  场景: 有界字典按 LRU 淘汰并计数
    假如 一个有界字典上限为 2
    当 依次写入 "a"、"b"，访问 "a"，再写入 "c"
    那么 有界字典保留 "a" 与 "c"
    而且 有界字典淘汰计数为 1

  场景: 写入调度的 drain 是确定性就绪信号
    假如 一个写入调度器 防抖窗口 10ms
    当 连续调度 5 次变更并 drain
    那么 实际写盘次数为 1
