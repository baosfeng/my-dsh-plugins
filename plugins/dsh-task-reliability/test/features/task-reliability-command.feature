# language: zh-CN
# 需求来源：GitHub issue + docs/任务可靠性/概述.md（需求 16：/task 斜杠命令，issue #35）
# 验收基准：host-command.mjs 的 Gherkin 化表达

功能: /task 斜杠命令
  作为 DSH 用户
  我想要在任何时候通过 /task 命令查看任务状态、继续任务、回答待确认问题、切换自主决策模式、注册任务
  以便 ask 挂起、锁屏、出远门等场景下也能方便地继续任务

  场景: 无参数显示任务状态
    假如 任务可靠性插件已启动
    当 我执行 /task 命令 ""
    那么 命令返回成功
    并且 命令输出包含 "任务可靠性状态"
    并且 命令输出包含 "活动任务: 0 个"

  场景: 显示活动任务与待确认问题
    假如 任务可靠性插件已启动且自主决策开启
    并且 会话 "s-1" 存在活动任务
    当 代理 "s-1" 调用 ask_user_question 且缓冲超时
    并且 我执行 /task 命令 "status"
    那么 命令返回成功
    并且 命令输出包含 "活动任务: 1 个"
    并且 命令输出包含 "待确认问题: 1 个"

  场景: continue 唤醒活动任务继续
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 存在活动任务
    当 我执行 /task 命令 "continue"
    那么 命令返回成功
    并且 代理 "s-1" 收到系统唤醒恢复指令

  场景: continue 无活动任务返回错误
    假如 任务可靠性插件已启动
    当 我执行 /task 命令 "continue"
    那么 命令返回失败
    并且 命令输出包含 "没有活动任务"

  场景: answer 回答待确认问题
    假如 任务可靠性插件已启动且自主决策开启
    当 代理 "s-1" 调用 ask_user_question 且缓冲超时
    并且 我回答待确认问题 "好的"
    那么 命令返回成功
    并且 问题 "需要确认" 已被回答

  场景: answer 问题不存在返回错误
    假如 任务可靠性插件已启动
    当 我执行 /task 命令 "answer q-missing 好的"
    那么 命令返回失败
    并且 命令输出包含 "回答失败"

  场景: autopilot on 开启自主决策模式
    假如 任务可靠性插件已启动
    当 我执行 /task 命令 "autopilot on"
    那么 命令返回成功
    并且 自主决策模式已开启

  场景: autopilot off 关闭自主决策模式
    假如 任务可靠性插件已启动且自主决策开启
    当 我执行 /task 命令 "autopilot off"
    那么 命令返回成功
    并且 自主决策模式已关闭

  场景: register 注册任务到当前会话
    假如 任务可靠性插件已启动
    当 我执行 /task 命令 "register 开发一个功能"
    那么 命令返回成功
    并且 任务列表包含该任务且状态为进行中

  场景: 未知子命令返回错误
    假如 任务可靠性插件已启动
    当 我执行 /task 命令 "foo"
    那么 命令返回失败
    并且 命令输出包含 "未知命令"
