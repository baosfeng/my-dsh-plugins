# language: zh-CN
# 需求来源：docs/任务可靠性/需求清单.md（需求 1-12）
# 验收基准：host-smoke.mjs / host-edge.mjs 的 Gherkin 化表达；新增需求须在此补充场景

功能: 任务可靠性保障
  作为 DSH 用户
  我想要任务在模型超时、提前结束、思考重复、系统休眠等情况下仍能可靠执行完成
  以便不盯着页面也能信任长任务会走完

  场景: 注册任务并持久化
    假如 任务可靠性插件已启动
    当 我注册会话 "s-1" 的任务 "开发一个功能"
    那么 任务列表包含该任务且状态为进行中
    并且 任务注册表已写入持久化文件

  场景: 同一会话重复注册被拒绝
    假如 任务可靠性插件已启动
    当 我注册会话 "s-1" 的任务 "开发一个功能"
    并且 再次为会话 "s-1" 注册任务 "另一个任务"
    那么 第二次注册返回失败

  场景: 模型超时错误自动重试
    假如 任务可靠性插件已启动
    当 代理 "s-1" 的模型请求以 TIMEOUT 失败
    那么 插件返回重试动作且不委托 next
    当 代理 "s-1" 的模型请求以 INVALID_ARGUMENT 失败
    那么 插件委托 next 处理

  场景: 任务未完成时自动继续
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 存在活动任务
    当 代理 "s-1" 的回合即将结束
    那么 插件向代理注入继续指令（steer）
    并且 指令包含任务描述

  场景: 无任务时不注入继续
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 没有活动任务
    当 代理 "s-1" 的回合即将结束
    那么 插件不注入任何指令

  场景: 子代理回合结束不受影响
    假如 任务可靠性插件已启动
    并且 子代理 "sub-1" 有活动任务
    当 子代理 "sub-1" 的回合即将结束
    那么 插件不注入任何指令

  场景: 循环次数达到上限后任务标记失败
    假如 会话 "s-1" 注册了活动任务且循环上限为 2
    当 代理 "s-1" 回合结束触发 3 次
    那么 任务状态变为 failed

  场景: 校验模式会话结束后校验完成度
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 注册了校验模式任务
    当 代理 "s-1" 变为空闲
    那么 插件创建独立校验代理
    并且 校验代理收到校验指令
    当 校验代理结论为已完成
    那么 任务状态变为 done

  场景: 校验结论未完成则唤醒主代理继续
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 注册了校验模式任务
    当 代理 "s-1" 变为空闲
    并且 校验代理结论为未完成并附原因
    那么 主代理收到带原因的继续指令

  场景: 思考重复流被检测并打断
    假如 任务可靠性插件已启动
    当 代理 "s-1" 的模型流产生连续重复的思考段落
    那么 流抛出思考循环错误
    当 代理 "s-1" 的回合即将结束
    那么 插件注入打断指令

  场景: 自主决策模式缓冲期内询问正常展示
    假如 任务可靠性插件已启动且自主决策开启
    当 代理 "s-1" 调用 ask_user_question 工具
    那么 工具流程的 next() 被调用
    并且 问题未被记录到待确认列表

  场景: 自主决策模式缓冲超时后自动决策
    假如 任务可靠性插件已启动且自主决策开启
    当 代理 "s-1" 调用 ask_user_question 且缓冲超时
    那么 问题被记录到待确认列表
    并且 代理收到自主决策指令

  场景: 自主决策缓冲禁用时立即拦截
    假如 任务可靠性插件已启动且自主决策开启且缓冲禁用
    当 代理 "s-1" 调用 ask_user_question 工具
    那么 工具被拒绝且原因含自主决策说明
    并且 问题被记录到待确认列表
    并且 工具流程的 next() 未被调用

  场景: 自主决策关闭时询问正常透传
    假如 任务可靠性插件已启动且自主决策关闭
    当 代理 "s-1" 调用 ask_user_question 工具
    那么 工具流程的 next() 被调用

  场景: 重启后活动任务自动恢复
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 注册了活动任务
    当 插件重新启动
    那么 代理 "s-1" 收到系统重启恢复指令
    并且 任务记录 resumeAt

  场景: 远程 hook 注册任务
    假如 任务可靠性插件已启动
    当 我通过远程 hook 注册会话 "s-1" 的任务
    那么 任务出现在任务列表

  场景: 远程 hook 无 token 被拒
    假如 任务可靠性插件已启动且配置了 apiToken
    当 我通过远程 hook 不携带 token 调用
    那么 返回 403

  场景: ask 超时后自动继续不挂起
    假如 任务可靠性插件已启动且 ask 超时为 20 毫秒
    当 代理 "s-1" 调用 ask_user_question 且用户长时间未回答
    那么 插件返回模拟回答且推荐选项被选中
    并且 代理收到用户长时间未响应继续指令
    并且 问题被记录到待确认列表

  场景: ask 超时禁用时正常等待用户回答
    假如 任务可靠性插件已启动且 ask 超时禁用
    当 代理 "s-1" 调用 ask_user_question 且用户回答 "B"
    那么 插件返回用户的真实回答
    并且 代理未收到继续指令

  场景: 任务停滞被看门狗唤醒
    假如 任务可靠性插件已启动且看门狗间隔为 20 毫秒
    并且 会话 "s-1" 注册了活动任务
    当 任务停滞超过阈值
    那么 代理 "s-1" 收到系统唤醒恢复指令

  场景: 普通对话输出截断自动补完（issue #147）
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 没有活动任务
    并且 代理 "s-1" 的最后输出不完整
    当 代理 "s-1" 的模型流以 max-tokens 结束
    并且 代理 "s-1" 的回合即将结束
    那么 插件向代理注入补完指令（steer）

  场景: 截断但输出完整不救场（issue #147）
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 没有活动任务
    并且 代理 "s-1" 的最后输出完整
    当 代理 "s-1" 的模型流以 max-tokens 结束
    并且 代理 "s-1" 的回合即将结束
    那么 插件不注入任何指令

  场景: 用户主动停止不自动继续（issue #147）
    假如 任务可靠性插件已启动
    并且 会话 "s-1" 注册了活动任务
    当 代理 "s-1" 的回合即将结束且信号已中止
    那么 插件不注入任何指令
