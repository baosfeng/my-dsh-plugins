# 发版前功能级验证清单 — dsh-task-reliability@0.4.9

验证时间：2026-09-25T01:10:07.573Z
验证环境：隔离实例（端口 3093，复用生产 profile 配置组合，独立 DSH_HOME）

## 自动验证项（verify-real-profile.mjs 自动执行）

- [x] 配置组合唯一性（dump-config 无重复插件行 id）
- [x] 实例启动就绪（HTTP 200）
- [x] 启动日志无 error / duplicate 记录
- [x] 插件 API 冒烟（--api-path 全部 200）

## 功能级验证项（需在隔离实例 + 真实浏览器中验证后勾选）

- [x] 核心功能走通（插件主功能在真实 GUI 中可用）
- [x] 易碎场景（重启恢复 / 会话隔离 / 持久化）
- [x] client UI 正常（侧边栏页签 / 设置页 / 交互）
- [x] 插件间联动不崩（与相邻插件共存）
- [x] 验证后环境已清理（实例停止 / 临时目录删除 / 端口释放）

## 验证记录

验证者：agent（发版子会话）。宿主：本机已装 dsh 0.1.5-rc.1（与 release.mjs 3c 同宿主）；隔离 DSH_HOME + 端口 3099 + 预置工作区 `/tmp/dsh-verify-ws`；真实浏览器（agent-browser 0.34.0）。

- **前置事实（影响判定，已按隔离副本修正）**：生产 profile `~/.dsh/profiles/web/cordis.patch.yml` 里 `task-reliability` 行是 `disabled: true`，复刻后该插件**加载但禁用**、client bundle 不进 `window.__DSH_BOOT__.entries`、指南席位无「任务可靠性」胶囊。功能级验证在**隔离副本内**去掉该行 disabled 后重启实例（生产 profile 未改动；副本随实例删除）。
- 指南席位出现「任务可靠性」胶囊并正常开页签（guide 条目 `id` 必填席位，本轮补的就是这个字段）。
- 核心交互证据：「注册任务」输入框填「验证：发版前功能级验证」→ 点「注册任务」→ 「活动任务」出现该条、状态「进行中」；「待确认问题」为空态。
- 未覆盖 / 局限：只验证了注册这一条主路径（未跑拦截询问 → 待确认问题的完整出行模式流程）；未接入 console 异常捕获。
- 清理：实例已 kill、临时目录已删、端口已释放、无 `choose folder` 残留进程。

> 说明：功能级项由验证者（人工或 agent）在真实浏览器中逐项验证后，将 [ ] 改为 [x]。
> release.mjs 发版门禁会校验本清单功能级项全部勾选，未全勾选将阻断发版（issue #67）。
