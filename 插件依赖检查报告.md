# 插件依赖检查报告

## 检查时间

2025年9月13日

## 检查目标

1. 检查插件是否有引用第三方依赖的必要性
2. 特别关注 `dsh-better-sidebar` 等官方已原生支持的能力
3. 检查依赖安装情况，确保用户安装插件时不会失败

## 检查结果

### 1. dsh-better-sidebar 依赖情况

**结论：已完全迁移，无需第三方依赖**

- ✅ **无 package.json 声明**：所有插件都没有在 `package.json` 中声明 `dsh-better-sidebar` 依赖
- ✅ **代码引用情况**：代码中的引用主要是注释和文档说明，说明插件已从 `dsh-better-sidebar` 迁移到官方 slots 服务
- ✅ **功能完整性**：所有侧边栏功能都使用官方 `ctx.slots` 服务实现，无需第三方依赖

**相关插件状态：**

- `dsh-my-guardian`：使用 `ctx.slots` 注册诊断面板（可选增强，不参与依赖声明）
- `dsh-my-plugin-manager`：使用 `ctx.slots` 注册管理页面
- `dsh-my-skill-manager`：使用官方 slots 扩展点
- `dsh-my-memory`：使用官方 slots 服务
- `dsh-file-activity`：已迁移到官方服务

### 2. dsh-shared 依赖情况

**结论：必要的内部共享工具包**

`dsh-shared` 是本仓库内的共享工具包（`plugins/dsh-shared/`），不是第三方依赖。

**依赖插件：**

1. `dsh-file-activity` - 文件活动监控
2. `dsh-md-render` - Markdown 渲染
3. `dsh-my-guardian` - 插件守护
4. `dsh-my-notify` - 通知系统
5. `dsh-my-remote` - 远程连接
6. `dsh-task-reliability` - 任务可靠性

**提供的核心功能：**

- **信任围栏**：`isTrustedApiRequest` - Host-header 安全验证
- **HTTP 工具**：`readJsonBody`、`writeJson`、`writeError` - JSON 读写
- **配置持久化**：`currentProfile`、`patchFileOf`、`writePatchConfig` - cordis.patch.yml 读写
- **原子写**：`atomicWriteJson`、`atomicWriteStats` - 安全文件写入
- **资源护栏**：`createResourceGuard`、`createProcessSampler` - 资源监控
- **有界容器**：`boundedMap`、`boundList` - 内存安全数据结构

**必要性**：这些功能在多个插件中重复使用，抽取为共享包消除了代码重复（issue #45）。

### 3. 第三方依赖检查

**结论：无必要第三方依赖**

| 依赖类型   | 依赖项                           | 插件            | 必要性                     |
| ---------- | -------------------------------- | --------------- | -------------------------- |
| 运行时依赖 | `dsh-shared`                     | 6个插件         | ✅ 必要（内部共享包）      |
| 对等依赖   | `cordis`                         | 所有插件        | ✅ 必要（DSH 框架）        |
| 对等依赖   | `react`                          | 7个插件         | ✅ 必要（UI 组件）         |
| 对等依赖   | `@deepseek-ai/dsh-session-title` | `dsh-my-notify` | ✅ 可选（会话标题功能）    |
| 开发依赖   | `@types/node`                    | 4个插件         | ✅ 必要（TypeScript 类型） |
| 开发依赖   | `typescript`                     | 4个插件         | ✅ 必要（类型检查）        |

### 4. 依赖安装情况

**结论：已正确安装，用户安装安全**

- ✅ **node_modules 存在**：所有有依赖的插件都已安装 `node_modules`
- ✅ **npm 自动安装**：用户安装插件时，npm 会自动安装 `dependencies` 中的依赖
- ✅ **无缺失依赖**：没有发现缺失的必要依赖

**安装流程：**

1. 用户运行 `npm install` 或 `dsh plugin install`
2. npm 读取 `package.json` 中的 `dependencies`
3. 自动安装 `dsh-shared` 及其依赖
4. 插件正常运行

### 5. 建议和优化

#### 已完成的优化：

1. ✅ 移除了 `dsh-better-sidebar` 第三方依赖
2. ✅ 迁移到官方 `ctx.slots` 服务
3. ✅ 抽取共享功能到 `dsh-shared` 包
4. ✅ 正确声明所有依赖

#### 可考虑的优化：

1. **文档更新**：更新插件文档，说明已迁移到官方服务
2. **依赖版本统一**：确保所有插件使用相同版本的依赖
3. **开发依赖优化**：考虑将 TypeScript 相关依赖移到根项目

## 总结

**当前状态：✅ 依赖管理良好**

- 无必要第三方依赖
- 已迁移所有 `dsh-better-sidebar` 功能到官方服务
- 依赖安装正确，用户安装插件时不会失败
- 共享功能通过内部包 `dsh-shared` 实现，避免代码重复

**风险：无**

用户安装插件时，所有必要依赖都会自动安装，无需手动干预。
